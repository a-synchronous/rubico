/**
 * @name callConcat
 *
 * @synopsis
 * callConcat(object Object, values any) -> object.concat(values)
 */
const callConcat = function (object, values) {
  return object.concat(values)
}

module.exports = callConcat
